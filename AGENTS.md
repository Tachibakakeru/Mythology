# World Mythology Genealogy — AGENTS.md

## 專案概覽

單一 HTML 檔案 (`index.html`) 的世界神話族譜視覺化工具，無建置系統。
- **入口**: `D:\YiHsiang\程式\Mythology\index.html`
- **本地伺服器**: port 7788
- **技術**: 純 HTML + CSS + JS（Embedded），無框架

---

## 架構

### 節點資料格式
```js
[id, name, kana_subtitle, role, col, row, desc, isNote?, tag?]
// 索引 0-6 必填；col/row 必為 number；role 必為 string（常見漏填錯誤）
```

### 神話體系註冊
```js
const PANTHEONS = {
  key: { title, defs, groups, unions, names, seed, storage, ... }
}
```

### CSS z-index 層級
| 層 | z-index |
|---|---|
| svg#edges | 1 |
| .node | 3 |
| .groupbox | 5 |
| .node.highlight | 6 |
| .edge-label | 7 |
| topbar | 10 |
| panel | 15 |
| searchResults | 20 |
| groupPickerList | 25 |
| regionFilterList | 30 |
| imgLightbox | 200 |

---

## 首頁 UI

- **3D CSS 旋轉木馬**（`transform-style: preserve-3d`, `rotateY`, `translateZ`）
- `allWheelCards`（全部卡牌）vs `wheelCards`（過濾後可見）
- `rebuildWheel()` 重算旋轉木馬排列、dots、角度

### 地區過濾器
- `data-region` 屬性在 `.myth-icon` 上
- 可選地區：`east_asia` / `south_asia` / `europe` / `middle_east` / `africa` / `modern` / `other`
- 持久化：`localStorage('mythRegionFilter')`（儲存隱藏地區的 JSON 陣列）
- 勾選／取消後清單必須維持開啟；**僅**點擊外部、Esc、或 toggle 按鈕才關閉
- 清單底部必須同時提供「全部顯示」與「全部取消」；允許所有地區被取消，旋轉木馬需安全處理零張可見卡牌，不能自行恢復顯示。
- 首頁與地區篩選清單的捲軸僅隱藏外觀、不得關閉原生滾動；Firefox／舊 Edge 與 WebKit 均需覆蓋。
- 騎士王／亞瑟王傳說屬於 `other`（其他傳說），不屬於 `europe`
- 印度神話屬於 `south_asia`（南亞），不可歸入 `other`（其他傳說）或 `east_asia`（東亞）。

### 圖譜工具列

- `#backBtn`／`#yBackBtn` 與同層頁面切換按鈕（`#toYokaiBtn`／`#toMythBtn`／`#toDemonBtn`）必須使用 `var(--accent)`，與頁面標題的藍色字體一致；此為共用 CSS，修改時需檢查所有體系。
- 圖譜／妖怪頁採共用的互動式藍色 UI 層：深藍格線畫布、半透明控制列、帶景深回饋的卡片、玻璃感詳情抽屜與藍色浮動縮放控制。首頁的 3D 轉盤機制與卡牌排列方式不得因這層改造而替換。
- `#stage`／`#ystage` 的平移必須用 `translate3d()`，以利瀏覽器合成層；雙擊空白畫布會平滑以游標位置放大，節點與關係標籤的雙擊不得觸發此行為。必須尊重 `prefers-reduced-motion`。
- 各圖譜主題由 `openPantheon(key)` 寫入 `#japan[data-pantheon]`，CSS 只覆寫 `--accent`、`--theme-wash`、`--theme-secondary`、`--theme-glyph`、`--node-radius` 等 token。日本、印度、天使、惡魔、希臘、北歐、埃及、美索不達米亞、波斯、凱爾特、亞瑟與克蘇魯都必須有獨立主題；不可複製十二套 DOM／JS。
- 禁止在整個 `#stage` 上使用 `filter`／`drop-shadow`，也不要為所有節點保留大範圍外陰影；大型系譜平移時會迫使瀏覽器重繪巨大合成圖層。陰影只留在 hover、highlight 與 key 節點。`prefers-reduced-motion` 不得覆蓋首頁 `#wheel` 或頁面 `.zoom-in/.zoom-out`，以免轉盤與淡入淡出變成瞬間跳轉。
- 響應式斷點：1100px 以下收合圖例；700px 以下工具列可橫向捲動、主畫布與妖怪畫布位移至 62px 標題列下方，`#panel`／`#ypanel` 必須是全寬底部抽屜並以 `translate3d(0,0,0)!important` 正確覆蓋桌面側滑狀態。每次改動新版抽屜 CSS 都要做 390px 實測。

---

## 邊線 / 關係線

### 點擊偵測
- 隱形 14px 寬 `path` 元素（`data-uid`）供點擊
- `.edge-label` 需 `pointer-events:auto; z-index:7`（必須高於 `.node.highlight` 的 6）
- Stage 委派點擊：`e.target.closest('.edge-label')` → `highlightUnion(uid)`

### 拖曳防護
```js
wrap.addEventListener('mousedown', e => {
  if (e.target.closest('.node, .edge-label')) return; // 防止背景拖曳
  dragging = true; ...
});
```
- Touch：`touchMoved` flag + 6px `TOUCH_DRAG_THRESHOLD`
- 主系譜畫布：`applyTransform()` 必須先執行 `clampGraphView()`；畫布小於視窗時固定置中，畫布大於視窗時四邊最多保留 40px 空白，禁止無限拖曳。

---

## 已收錄神話體系

| key | 名稱 | 節點數 |
|-----|------|--------|
| japan | 日本神話 | 267 |
| india | 印度神話 | 95（含神獸、神器、聖典與宇宙事件群組） |
| buddhism | 佛教世界 | 247（佛傳、教法、弟子、主要傳承、經律論、諸佛菩薩、諸天、二十八部眾、胎藏界、明王、護法神與宇宙觀） |
| angels | 天使系譜 | 128（三級九等、聖經異象、七重天、生命樹、伊斯蘭與跨傳統名錄） |
| demons | 惡魔圖鑑 | 119（含《戈埃提亞》完整七十二靈、七宗罪及墮天守望者名錄） |

印度神話資料規則：以《羅摩衍那》、《摩訶婆羅多》、往世書及吠陀傳統為分層來源；十化身、女神形態與神祇親屬若有教派／地域異說，必須在節點描述中標明，不能寫成唯一正統。事件、師徒與政治同盟一律用 `partner` 與清楚標籤，不得偽裝成婚姻或血緣。
佛教頁採跨傳統導覽而非單一神譜：早期佛傳與教法、大乘佛菩薩、密教尊格、天龍八部、六道宇宙觀及漢藏經典必須分層標示。巴利、梵文、漢譯、藏譯與地方藝術的異說不可合併成所有佛教共有設定；歷史人物血緣可用 `parent`／`primary`，教法、授記、脅侍、護法與經典關聯一律使用 `partner` 或具名事件關係。
佛教節點來源使用 `BU_NODE_SOURCES`／`BU_SOURCE_LIBRARY`，優先連至 SuttaCentral、BDK English Tripiṭaka 與 84000 等經典或專業翻譯資料。來源不足時不可自行補造故事；後世佛傳、註釋、地方信仰與早期經藏內容須在敘述中明確區分。
佛教護法是跨傳統職能，不得整理成唯一固定神系。藥師十二神將以玄奘譯《藥師經》十二名與各領七千藥叉的誓願為準，十二生肖須標成日本後世配對；藏傳護法須區分智慧／出世間與世間誓願護法，教派名冊與化現異說不可混寫。伽藍神是寺院護法泛稱，關帝伽藍與八幡大菩薩須明示為東亞地方歷史及神佛習合發展。
神性父系與人間母系同時存在時，必須各自保留一條 `parent` 關係；例如迦爾納同時連結蘇利耶與昆蒂，不得因事件敘事而漏掉任一血緣。
天使與惡魔採兩個 `PANTHEONS` 圖譜頁，而非將不同傳統硬併為同一系譜；共有的墮天／對立角色必須保有同 ID，並以 `p-counterpart`／`openCounterpart()` 在面板中切換。九階天使、守望者、所羅門系統與伊斯蘭精靈必須註明所屬文本或後世傳統，禁止互相當成同一正典設定。
使用者提供的墮天／守望者名單，若同時需要在天使與惡魔兩頁查閱，必須以同一 ID 同步建立節點、別稱、來源與跨頁切換；《以諾一書》文本條目、後期彙編與《失樂園》角色必須分組標示。伊圖里爾、澤豐雖可因名錄需求收錄於惡魔頁，但必須明示其為《失樂園》的天使而非墮天守望者。
天使／惡魔資料須額外提供「舊約／希伯來聖經」、「新約／啟示錄」與「次經／第二聖殿文獻」的交叉群組，讓同名或後期等同的角色能按出處比較。撒但在新約中提到的「魔鬼與其使者」不得補造成具名部屬名冊；可加入《馬可福音》的「群／軍團」案例，但必須明示經文未將其固定編入撒但麾下。
九階天使在本專案固定以偽狄奧尼修斯系統顯示為三級九等：上三「神聖階級」（熾天使、智天使、座天使）、中三「子階級」（主天使、力天使、能天使）、下三「聖靈階級」（權天使、天使長、天使）；若引用其他排序，必須明記為異說。
具名天使的節點角色欄與敘述必須附上名稱的常見語源寓意；詞源具爭議或屬後期轉寫時必須標示「通常解作」或「異說」，不可把民間語源寫成定論。《戈埃提亞》必須保留獨立書籍節點，群組成員數必須恰為 72 且不可重複。
《戈埃提亞》72 靈的面板必須逐柱呈現序號、軍團數、原典所述形象、職掌與個別印章；印章限用可追溯至公版原書掃描的圖檔，並連回圖檔典藏頁。七宗罪採賓斯費爾德（1589）分類時，必須明記為後期惡魔學，非正典或跨教派共識。七美德與天使的配對亦須標示為本圖的寓意展示，不得聲稱有正典指定。以賽亞6章的熾天使與以西結1、10章的基路伯／活物／多眼輪必須分文本標示，不能互相替代。
附圖必須維持既有「點擊放大」行為；需要出典時應把外部來源連結放在圖說，不能把圖片本體包成會搶走燈箱點擊的連結。
具名天使的面板僅在有可追溯的歷史藝術或聖像圖時附圖；禁止以生成圖、泛用天使圖或無授權網圖冒充該角色的史料形象。
不可為了「每位都有圖」而重複套用泛用天使圖。沒有可靠的個別圖像時，面板應不顯示附圖。經文異象圖的圖說必須標明所屬文本、年代／教會／畫家等可確認來源，並註記為該藝術品或文本詮釋，而非各傳統唯一形象。熾天使的《以賽亞書》描述是每位六翼（三對），不可誤記為每位十二翼。
跨頁節點切換要在 `openPantheon()` 的初始 `fitInitial()` 之後執行 `focusNode()`；不可同步呼叫而讓初始縮放覆蓋節點聚焦。
群組框必須以真實節點位置決定，而非只服從資料上舊的 `drawBox=false` 註記：節點外接範圍合理時一律畫框；範圍過大、或混入的非成員比群組成員多時，改為 `marker-only` 標記，並沿用既有 `highlightGroup()` 的成員顯示／其餘淡化行為。此規則適用所有主圖譜。

印度節點面板必須使用 `IN_NODE_SOURCES` 與 `IN_SOURCE_LIBRARY` 顯示至少一項來源連結；吠陀神用《梨俱吠陀》，兩大史詩人物分別連到《羅摩衍那》或《摩訶婆羅多》，《薄伽梵歌》另附其專屬文本。
節點描述若明確援引兩種文本，來源面板也必須顯示兩者；例如摩訶缽特摩同時對應往世書的八大那伽脈絡與《摩訶婆羅多》蛇族名錄。
印度頁的物件、神獸與文獻必須以獨立群組呈現，也可同時出現在人物／史詩群組；不可因為是「非人物」而省略來源、敘事描述或關係標籤。
調整節點座標後，必須以 `COLW=185`、卡片寬 `NODE_W=150` 與實際卡片高度檢查碰撞；不得只憑格位不同就假設不會重疊。優先修正資料座標，非必要不改共用佈線或縮放邏輯。
搜尋命中節點時，視角置中必須扣除已開啟的右側資訊面板寬度；不得以整個 `canvasWrap` 置中而使目標被面板覆蓋。
圖譜平移邊界同樣要扣除右側面板寬度；只修正 `focusNode()` 不足以處理位於圖譜右緣的節點，因其置中結果會被共用邊界鉗制。
| greek | 希臘神話 | 323 |
| norse | 北歐神話 | 177 |
| mesopotamia | 美索不達米亞神話 | 98 |
| persia | 波斯祆教 | 71 |
| celtic | 凱爾特神話 | 80 |
| arthurian | 騎士王・亞瑟王傳說 | 45 |
| cthulhu | 克蘇魯神話 | 107 |
| egypt | 埃及神話 | 70 |

### 日本妖怪圖鑑

- 目前 93 個節點、14 個群組；以 `YOKAI_DEFS` 與 `YOKAI_GROUPS` 驅動，不納入 `PANTHEONS` 主系譜節點數。
- 「百鬼夜行・絵巻」是繪卷主題群組，而非固定的百名妖怪名冊；群組只收錄與付喪神／百鬼夜行圖像傳統有明確關聯的代表節點，避免把不同版本混作唯一名單。
- 橋姬僅保留一個 `hashihime` ID；其描述需保留橋神與嫉妒鬼女兩條傳承的差異，禁止重複定義同一 ID。

### 日本頁語言切換

- 語言切換只可出現在日本神話系譜與日本妖怪圖鑑；狀態以 `localStorage('japanLocale')` 保存，值為 `zh-TW` 或 `ja`，不得影響其他神話頁。
- `JP_UI` 管理日本頁工具列、搜尋、面板欄位與收藏等固定文字；`localGroupLabel()` 管理日本神話／妖怪群組的繁中、日文顯示名稱。切換後必須重繪目前頁面的群組與已開啟面板。
- 第一階段已完成介面及群組在地化。故事敘述採既有原文優先；新增另一語言敘述時應以獨立語言覆蓋資料保存，不得直接覆寫原始 `desc` 或使用者編輯。
- 敘述覆蓋分為 `JP_DESC_JA`（日文）與 `JP_DESC_ZH`（繁中）；`layoutNodes()` 依日本神話頁的目前語系選用對應覆蓋，且只有沒有 `ov.desc` 時才套用。使用者自訂敘述永遠優先，不得因切換語言遭覆寫。
- 日文覆蓋尚未逐筆展開時，必須使用 `japaneseFallbackDesc(name, role)` 產生可讀的日文說明，不能回退成繁中原文；逐筆的 `JP_DESC_JA` 仍優先，並應持續取代該通用回退。
- 妖怪頁以 `YOKAI_BASE_DESCS` 保留日文原始敘述，繁中逐筆覆蓋放在 `YOKAI_DESC_ZH`。`applyYokaiLocale()` 必須在開啟妖怪頁與切換語言時執行，且 `YOKAI_EDITS_LIVE[id].desc` 永遠優先；尚未逐筆翻譯的節點不可回退日文，使用 `yokaiFallbackZh()` 的中立中文並保留資料庫來源。
- 短敘述審核的下限為 40 個字元；`strengthenShortZh()` 對日文原文的繁中覆蓋及妖怪繁中覆蓋補上「版本差異＋面板出典」提示。不得改動 `ov.desc`，也不得拿此共用提示代替後續可查證的個別故事內容。
- 補譯須以節點面板的來源連結可追溯之內容為準；若來源僅能佐證共同神話段落，`PROGRESS.md` 必須如實標明為共同文本脈絡，不得寫成個別專條佐證。
- 日本神話繁中模式不只面板敘述需使用繁中：卡片假名必須隱藏、角色欄須由 `JP_DESC_ZH` 的繁中摘要輸出，關係標籤必須透過 `JP_RELATION_ZH` 對照；新增日文關係標籤時，須同步補入繁中對照並驗證切換後沒有日文殘留。
- 經來源審核的日本神話節點以 `JP_NODE_SOURCES` 對應 `JP_SOURCE_LIBRARY`；`openPanel()` 將來源連結渲染至 `#p-sources`。來源 URL 必須指向原典或學術／典藏機構頁面，禁止用未驗證的概述網站替代。
- 瓊瓊杵尊與木花開耶姬的御子須區分記紀版本：《日本書紀》本書的火闌降命→彥火火出見尊→火明命只能標成「本書的一例」，不可視為各版本共同次序；其他一書還有二子、三子、四子及不同排列，《古事記》則為火照命→火須勢理命→火遠理命，一書第八更把火明命列為瓊瓊杵尊兄長。頁面須在既有「火中誕生・海幸彦と山幸彦」故事節點集中對照，不得混寫成單一固定次序。
- 天香山命系譜須保留文本差異：`amenokaguyama` 連至火明命與尾張氏等後裔；高倉下在《先代旧事本紀》為同一視傳承，在《海部氏勘注系図》另有父子系譜，關係線與敘述都不可把任一版本寫成唯一史實。

**首頁 disabled（未完成）**: Angels & Demons（天使惡魔）

---

## 克蘇魯神話分組（17 組）

外神、舊日支配者、古神、長老神與舊神、地球原有的眾神、其他超自然生靈、
雙生舊日支配者、侍從種族、夢境國度生物、典籍與神器、重要地點、重要人物、
4 個故事群組、創作者群組

---

## 常見錯誤與防護

| 錯誤 | 症狀 | 預防 |
|------|------|------|
| 缺 role 欄位 | col/row 被字串佔用，所有座標錯位 | 驗證 `typeof d[3]==="string" && typeof d[4]==="number"` |
| `const PANTHEONS` 被吃掉 | 語法錯誤，頁面空白 | 插入時確認 `const PANTHEONS = {` 仍在 |
| edge-label z-index 過低 | 點擊高亮節點時無法選到文字框 | 保持 z-index:7 |
| mojibake（亂碼） | kana 欄位含西里爾/韓文字元 | 驗證腳本掃描非 ASCII |

### 驗證指令
```bash
node -e "/* 9-類驗證腳本 */"
# 檢查：語法、節點數、重複ID、欄位型別、壞group/union參照、重複座標、mojibake
```

---

## 更新規則

- 每次回答後即時更新此檔案（新增節點數、修正記錄）
- 若新增神話體系，在「已收錄神話體系」表格補一行
- 若修正 CSS/JS 關鍵邏輯，在對應章節更新範例

---

## AGENTS 工作規則

- 本檔案完整沿用 `CLAUDE.md` 的核心架構、UI 設計、資料格式、互動規則、驗證方式與更新規則；後續工作必須遵循。
- 除非使用者明確要求，禁止任意改動既有核心架構、UI 互動模式、z-index 層級、資料格式或單檔案架構。
- 若判斷有更好的架構、UI 設計或實作方式，先說明現況、替代方案與影響，等待使用者決定後才執行；不得自行套用。
- 每次完成任何程式、資料、CSS、JS 或 UI 改動後，都要同步更新 `AGENTS.md` 與 `PROGRESS.md`。
- `PROGRESS.md` 的修正記錄必須詳細描述：日期、改動檔案、改動位置或功能、問題／需求、採用方式、驗證結果，以及尚未完成或可能影響的事項。
- 若新增節點、神話體系、群組或關係線，必須同步更新對應數量、清單與進度狀態。
- 變更前先讀取相關程式區段與目前文件；變更後執行適當驗證，並在 `PROGRESS.md` 記錄結果。
- 保持最小變更範圍，不新增不必要的框架、依賴、建置系統或抽象層。
- 主圖譜搜尋結果必須讓正規化後的節點名稱完全相符項目優先於別稱、角色、神社與敘述命中；否則同名異傳的別稱節點會攔截精確搜尋。
- 大型圖譜的常態動畫只允許使用 `transform`／`opacity`；禁止在整個 stage、所有連線、群組框、移動中的詳情面板或大量節點上套用 `filter`、`backdrop-filter` 或大範圍動態陰影。彈跳與發光只能套在目前選取／hover 的少量元素。
- 首頁進入圖譜時必須先完成資料與佈局，再讓圖譜以 opacity 疊在仍可見的首頁上；圖譜完全顯示後才隱藏首頁，避免背景閃光。不得依賴背景分頁可能暫停的 `requestAnimationFrame` 作為唯一完成機制。
- 所有主圖譜節點由共用 `nodeAvatarText()`／`nodeAvatarHue()` 提供輕量且可辨識的名稱／職能徽章；獨立原創插畫統一登記於 `NODE_PORTRAITS`。節點用圖片需先縮至網頁尺寸，且面板必須標示「原創節點插畫・非史料形象」，不得與有出處的歷史圖像混淆。
- 首頁與圖譜之間可保留整頁淡入淡出；共用容器內的體系切換（如天使／惡魔）及日本神話／妖怪圖鑑切換必須使用 `showViewImmediately()` 維持 opacity 1，不得把完整視窗先降至透明再重建，以免出現黑白閃光或空白幀。所有整頁動畫結束時必須清除 inline `transition`。
- 日本神話原創節點插畫目前完成 75 個節點；第八批新增天岩戶、三神器、國讓、神武東征、黃泉、八雷神、浦島、竹取、惠比壽、熊野、金剛藏王與天日槍。完整名單以 `NODE_PORTRAITS` 為準；人物、神獸、故事與神器皆沿用 256×256 圓形透明 PNG 與相同圖說規格，加入前須檢查 alpha 外緣並同步更新本檔與 `PROGRESS.md`。原典沒有固定外貌的原初神應優先採抽象象徵，或在原創人物化提示與進度中明確註明非公認形象。
- 出雲的「神議り／神議」必須標成神在祭的祭祀傳承，不能寫成《古事記》或《日本書紀》的獨立故事；節點須連到大國主神，並以出雲大社官方神在祭資料佐證稻佐之濱、十九社、上宮與神等去出祭脈絡。
- 希臘不死鳥須區分希羅多德《歷史》2.73、奧維德《變形記》15.391–407與後世浴火重生形象；`GR_NODE_SOURCES`／`GR_SOURCE_LIBRARY` 負責希臘節點的逐節點來源，不得把後世灰燼意象寫成所有早期文本共有細節。
- 亞瑟王資料以傑弗里・蒙茅斯、法國羅曼史／散文循環與馬洛里《亞瑟之死》的綜合傳統為基礎；遇到血緣、人物身分、神器來源或事件結局有版本差異時，必須在節點描述中標示，而非寫成唯一史實。
- 「加百列三次下凡／顯現」必須標為依《但以理書》8–9章及《路加福音》1章形成的常見敘事整理，不可寫成聖經明定的術語或固定次數；第一項使命需說明它合併了《但以理書》的兩段啟示。
- 佛教「八方天／十二天」採《大日經疏》系方位護法名單；「十六／二十／二十四諸天」採漢傳寺院常見護法名冊並明示異本，不能把兩套系統寫成全球佛教唯一、直線擴編的編制。千手觀音二十八部眾依 CBETA T1060 的二十八段護法名號呈現，一部可含多人或一類眾生，並註明三十三間堂等寺院造像名單差異。
- 胎藏界採《大日經》系十二大院與中臺八葉院九尊框架，寶幢、開敷華王、無量壽、天鼓雷音四佛不可直接換成金剛界五佛。五大明王採東密常見方位系統；八大明王須標出所依《大妙金剛》系或《秘藏記》系異本，不混成單一固定名單。
- 主圖譜放大仍使用 CSS transform 以維持平移效能，但禁止對整個 `#stage` 常駐 `will-change:transform` 或 `translate3d()`；位移需對齊裝置像素，平滑縮放 transition 結束後立即移除 `.smooth`，避免 Chromium 長期沿用低倍率 GPU 點陣快取造成文字模糊。妖怪圖鑑 `#ystage` 同樣遵守。
- 親子／分類關係線只能從節點外框終止：若父節點與子節點同列，或父節點位於子節點下方，禁止由較低的橫向匯流線回折穿入子節點頂邊；必須改走側邊外框。同欄多節點的梳狀主幹必須保留連到首個短枝的端點，再延伸至最末短枝，不可先刪除首端。共用路由的安全橫帶搜尋需覆蓋父子實際列距；修改後除以全部 `.edge` 路徑對節點內框做幾何交叉掃描，也要檢查每條非婚姻短枝端點是否與同一關係的主幹相交，避免「不穿框但視覺斷線」。
- 「圓桌十二騎士」採目前資料的代表性十二人名單：亞瑟、凱、蘭斯洛特、加拉哈德、高文、加雷斯、阿格拉瓦因、伊凡、貝德維爾、帕西法爾、鮑斯、崔斯坦；此名單並非所有中世紀文本共通的固定名單，群組以黯淡其餘節點的模式呈現，不畫跨距外框。
- 現有資料補強優先順序：日本神話（早期短敘述最多）→ 希臘／北歐 → 埃及／美索不達米亞／波斯；補寫時維持既有 ID、座標、群組與關係線，優先擴充描述，不任意改動血緣結構。
- 全站關係審核（2026-08-22）確認既有 9 個體系沒有失效的 `parent`／`children`／`spouses` 參照。對概念、事件或傳承人物新增連線時，使用 `marriage:"partner"` 加註事件標籤，不能冒充正式婚姻或親子血緣。
